name: my_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # --- Core/UI ---
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10
  lottie: ^3.1.2
  animated_splash_screen: ^1.3.0
  page_transition: ^2.1.0

  # --- Networking/Storage/Charts ---
  http: ^1.2.2
  shared_preferences: ^2.5.3
  fl_chart: ^1.0.0

  # --- Camera/QR ---
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.0.1
  permission_handler: ^12.0.1
  # --- Calendar ---
  table_calendar: ^3.0.9

  # --- Files / Viewer ---
  file_picker: ^8.0.0           # ✅ เหลืออันเดียว
  open_filex: ^4.4.0
  syncfusion_flutter_pdfviewer: ^31.1.23

  # --- i18n ---
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # ถ้าใช้ http ในโค้ดโปรดักชันแล้ว ไม่ต้องใส่ http ซ้ำใน dev_dependencies
  # http: ^1.1.0   # ❌ ลบออก เพื่อไม่ให้สับสนเวอร์ชัน

flutter:
  uses-material-design: true
  assets:
    - assets/login1.png
    - assets/signup1.png
    - assets/logounimai.png
    - assets/forgot1.png
    - assets/loader.json
    - assets/bell.svg
    - assets/check.svg
    - assets/num_student.svg
    - assets/arrow.svg
    - assets/time.svg
    - assets/add.svg
    - assets/plus.svg
    - assets/exclamation.svg
    - assets/file.svg
    - assets/calendar.svg
    - assets/check_circle.svg
    - assets/fileupload.svg
    - assets/fix.svg
    - assets/history.svg
    - assets/home.svg
    - assets/logout.svg
    - assets/map.svg
    - assets/piechart.svg
    - assets/profile.svg
    - assets/qr_code.svg
    - assets/search.svg
    - assets/cross_circle.svg
    - assets/document.svg
    - assets/bookplus.svg
    - assets/scan.svg
    - assets/status.svg
    - assets/check_status.svg
